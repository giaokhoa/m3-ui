import * as token from '@m3-ui/tokens';
import { pxNumber } from '../../internal/tokenValues';

export const switchStateLayerRadius =
  pxNumber(token.ComponentSwitchStateLayerSize) / 2;
export const switchTrackFocusRingRadius =
  `${pxNumber(token.ComponentSwitchTrackHeight) / 2}px`;
