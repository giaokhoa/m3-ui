{
  "name": "@m3-ui/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api:generate": "node scripts/build-api-reference.mjs",
    "spec:generate": "node scripts/build-material-spec.mjs",
    "docs:lint": "node scripts/docs-conformance.mjs",
    "dev": "pnpm api:generate && pnpm spec:generate && node scripts/build-docs-data.mjs && vite --host 0.0.0.0 --port 4173 --strictPort",
    "build": "pnpm api:generate && pnpm spec:generate && node scripts/build-docs-data.mjs && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173 --strictPort",
    "test": "pnpm api:generate && pnpm spec:generate && pnpm docs:lint && node --test scripts/*.test.mjs",
    "typecheck": "pnpm api:generate && pnpm spec:generate && node scripts/build-docs-data.mjs && tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist src/generated/docs-navigation.json src/generated/api-reference.generated.json src/generated/material-spec.generated.json public/search-index.json"
  },
  "dependencies": {
    "@m3-ui/ui": "workspace:*",
    "fumadocs-core": "16.15.3",
    "fumadocs-mdx": "15.4.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/mdx": "2.0.14",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
