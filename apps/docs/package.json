{
  "name": "@m3-ui/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api:generate": "node scripts/build-api-reference.mjs",
    "spec:generate": "node scripts/build-material-spec.mjs",
    "examples:generate": "node scripts/build-live-examples.mjs",
    "docs:lint": "node scripts/docs-conformance.mjs",
    "dev": "pnpm api:generate && pnpm spec:generate && pnpm examples:generate && node scripts/build-docs-data.mjs && next dev --hostname 0.0.0.0 --port 4173",
    "build": "pnpm api:generate && pnpm spec:generate && pnpm examples:generate && node scripts/build-docs-data.mjs && next build",
    "start": "next start --hostname 0.0.0.0 --port 4173",
    "test": "pnpm api:generate && pnpm spec:generate && pnpm examples:generate && pnpm docs:lint && node --test scripts/*.test.mjs",
    "typecheck": "pnpm api:generate && pnpm spec:generate && pnpm examples:generate && node scripts/build-docs-data.mjs && tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf .next .source dist src/generated/docs-navigation.json src/generated/api-reference.generated.json src/generated/material-spec.generated.json src/generated/live-examples.generated.json public/search-index.json"
  },
  "dependencies": {
    "@m3-ui/ui": "workspace:*",
    "fumadocs-core": "16.15.3",
    "fumadocs-mdx": "15.4.0",
    "next": "16.3.3",
    "react": "^19.0.0",
    "react-aria-components": "^1.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/mdx": "2.0.14",
    "@types/node": "22.20.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
