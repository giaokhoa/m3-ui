# Component documentation

Component pages in this directory document the Material contract implemented by `@m3-ui/ui`.

Keep parity metadata centralized in `apps/docs/src/componentDocs.ts`, render live examples with the public package, and keep adaptive layout policy out of component documentation.
