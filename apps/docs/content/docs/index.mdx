---
title: m3-ui documentation
description: Material 3 React components with canonical tokens, web semantics, and audited Compose parity.
---

## Material 3 is the contract

The documentation surface uses the same `@m3-ui/ui` components, theme roles, and generated typography tokens as consumer applications. Fumadocs provides the content pipeline; it does not define the visual language.

<ComponentPreview>
  <Button>Continue</Button>
  <ElevatedButton>Elevated</ElevatedButton>
  <FilledTonalButton>Learn more</FilledTonalButton>
  <OutlinedButton>Details</OutlinedButton>
  <TextButton>Cancel</TextButton>
</ComponentPreview>

## Foundations and theming

The implemented Material foundation guides are deliberately narrower than the full Material specification:

- [Color](/docs/foundations/color) — semantic roles, baseline schemes, dynamic color, and contrast level.
- [Typography](/docs/foundations/typography) — standard Material roles and M3 Expressive emphasized roles.
- [Theming](/docs/theming) — `ThemeProvider`, app-level system preference, ripple focus, and theme-aware portals.
- [Foundation index](/docs/foundations) — implemented scope and explicit omissions.

Shape, motion, and elevation are not published as global theme docs until equivalent reusable runtime contracts exist in `m3-ui`.

## Component guides

Start with the first Material component pages:

- [Button](/docs/components/button)
- [Text field](/docs/components/text-field)
- [Dialog](/docs/components/dialog)
- [All component guides](/docs/components)

Each guide records the Material family, Compose mapping, implementation boundary, accessibility behavior, and intentional browser adaptation before documenting examples and API details.

## Headless documentation engine

`fumadocs-mdx` compiles the MDX source and provides browser-safe Macro collections. This app intentionally does not import `fumadocs-ui`, its preset CSS, or a parallel design system.

```tsx
const page = getDocsPage(slugs);
const MDX = page.body;

<MDX components={getMdxComponents()} />;
```

## Layout boundary

> Documentation owns content composition, not Material adaptive-layout policy.

The docs app does not define its own compact, medium, or expanded breakpoints; pane directives; navigation rail switching; or scaffold algorithms. Those concerns must compose the existing `@m3-ui/ui/layout` subsystem as that work lands.

## Typography boundary

Semantic Markdown remains semantic HTML. Headings and body content are mapped to Material type roles at the MDX boundary instead of introducing documentation-only typography components into the core library.
